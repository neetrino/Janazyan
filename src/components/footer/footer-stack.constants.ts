/**
 * Footer shell stacks above storefront page layers (catalog/hero z-20).
 * Below sticky header (60) and mobile bottom nav (70).
 */
export const FOOTER_SHELL_Z_INDEX_CLASS = 'z-40';

/** Internal footer decoration — above footer surface, within shell context. */
export const FOOTER_DECORATION_Z_INDEX_CLASS = 'z-[1]';

/** Internal footer links and copy — above decoration. */
export const FOOTER_CONTENT_Z_INDEX_CLASS = 'z-10';
