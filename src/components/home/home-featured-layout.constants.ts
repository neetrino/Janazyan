/** Featured products in the mobile 2-column grid (phones). */
export const HOME_FEATURED_MOBILE_GRID_COUNT = 4;

/** Featured products in one row on tablet/laptop (768px–1299px). */
export const HOME_FEATURED_TABLET_ROW_COUNT = 3;

/** Featured products in one row on desktop (≥1300px). */
export const HOME_FEATURED_DESKTOP_ROW_COUNT = 4;
