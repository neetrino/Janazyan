/** Keeps instant search above mobile content cards and checkout layers. */
export const SEARCH_DROPDOWN_Z_INDEX_CLASS = 'z-[200]';

/** Mobile top bar — spans the full chrome row width. */
export const SEARCH_DROPDOWN_MOBILE_PANEL_CLASS =
  'absolute inset-x-0 top-full mt-2';

/** Desktop header popup — wider results panel. */
export const SEARCH_DROPDOWN_DESKTOP_PANEL_WIDTH_CLASS = 'w-[min(92vw,640px)]';
