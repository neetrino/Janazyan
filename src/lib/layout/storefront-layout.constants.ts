/** Responsive horizontal gutter for storefront blocks — keeps side space at every breakpoint. */
export const STOREFRONT_HORIZONTAL_GUTTER_CLASS = 'px-3 sm:px-4 lg:px-6';

/** Extra inner inset for card rows inside full-width section backgrounds. */
export const SECTION_CARD_ROW_INSET_CLASS = 'px-6 lg:px-12 xl:px-16';
